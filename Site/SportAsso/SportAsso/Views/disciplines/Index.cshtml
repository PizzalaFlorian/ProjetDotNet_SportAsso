@model IEnumerable<SportAsso.discipline>

@{
    ViewBag.Title = "Index";
}

<h3 class="center">Liste des disciplines</h3>

<p>
    @Html.ActionLink("Ajouter une disciplines", "Create", "disciplines", null, new { @class = "btn btn - default" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.label)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.utilisateur.login)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.utilisateur.login)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", "disciplines", new { id = item.discipline_id }, new { @class = "btn btn - default" }) 
            <br/>
            <br/>
            @Html.ActionLink("Supprimer", "Delete", "disciplines", new { id = item.discipline_id }, new { @class = "btn btn - default" })
        </td>
    </tr>
}

</table>
